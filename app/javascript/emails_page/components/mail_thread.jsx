import React, { Component } from "react"
import ReactDOM from "react-dom"

class MailThread extends Component {
  constructor(props) {
    super(props)
    const initialState = $.extend(this.props, {})
  }

  render(){
    return(
      <div></div>
    )
  }
}

export default MailThread


